'use client';import{useEffect,useState}from'react';export default function Login(){const[p,setP]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{fetch('/api/auth/session',{cache:'no-store'}).then(async r=>{if(r.ok&&(await r.json()).authenticated)location.href='/'}).catch(()=>{})},[]);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(r.ok){location.href='/';return}setError(r.status===401?'Mot de passe incorrect.':'Accès sécurisé indisponible. Vérifiez la configuration du serveur.')}catch{setError('Connexion impossible.')}finally{setBusy(false)}}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,background:'#07111f'}}><form onSubmit={submit} style={{width:'min(420px,100%)',background:'#fff',borderRadius:20,padding:32,boxShadow:'0 24px 70px rgba(0,0,0,.25)'}}><p className="eyebrow">RAPID PARE-BRISE · CRM</p><h1 style={{margin:'8px 0 10px'}}>Accès sécurisé</h1><p className="muted">Entrez le mot de passe du CRM pour accéder aux données commerciales.</p><label style={{display:'block',marginTop:24,fontWeight:700}}>Mot de passe<input autoFocus required type="password" autoComplete="current-password" value={p} onChange={e=>setP(e.target.value)} style={{display:'block',width:'100%',marginTop:8,padding:'13px 14px',border:'1px solid #d8dee7',borderRadius:10,font:'inherit'}}/></label>{error&&<p style={{color:'#b4232d',marginTop:12}}>{error}</p>}<button className="primary" disabled={busy} style={{width:'100%',marginTop:20}}>{busy?'Vérification…':'Se connecter'}</button></form></main>}

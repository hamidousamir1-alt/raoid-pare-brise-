'use client';import{useEffect,useMemo,useRef,useState}from'react';type Doc={id:string;name:string;fileName:string;mimeType:string;sizeBytes:number;category:string;createdAt:string};const icon=(t:string)=>t==='Présentation'?'▤':t==='Offre commerciale'?'◇':t==='Support RDV'?'◎':'⌁';export default function Documents(){const[q,setQ]=useState(''),[filter,setFilter]=useState('Tous'),[docs,setDocs]=useState<Doc[]>([]),[blocked,setBlocked]=useState(true),input=useRef<HTMLInputElement>(null);async function load(){try{const r=await fetch('/api/documents',{cache:'no-store'});if(r.status===401){location.href='/login';return}if(!r.ok){setBlocked(true);return}setDocs((await r.json()).items||[]);setBlocked(false)}catch{setBlocked(true)}}useEffect(()=>{load()},[]);const visible=useMemo(()=>docs.filter(d=>(filter==='Tous'||d.category===filter)&&`${d.name} ${d.category} ${d.fileName}`.toLowerCase().includes(q.toLowerCase())),[docs,q,filter]);async function upload(file?:File){if(!file||blocked)return;const name=prompt('Nom du document',file.name.replace(/\.[^.]+$/,''));if(!name)return;const category=prompt('Catégorie : Présentation, Offre commerciale, Support RDV ou Fiche pratique','Présentation')||'Fiche pratique';const f=new FormData();f.set('file',file);f.set('name',name);f.set('category',category);try{const r=await fetch('/api/documents',{method:'POST',body:f});if(r.status===401){location.href='/login';return}if(!r.ok){alert('Document non enregistré. Formats acceptés : PDF, PNG, JPG, DOCX, PPTX, 10 Mo maximum.');return}await load()}catch{alert('Stockage sécurisé indisponible. Aucun document local n’a été créé.')}}function open(d:Doc){window.open(`/api/documents/${d.id}`,'_blank','noopener,noreferrer')}async function share(d:Doc){const url=`${location.origin}/api/documents/${d.id}`;try{if(navigator.share){await navigator.share({title:d.name,url});return}await navigator.clipboard.writeText(url);alert('Lien copié. Le destinataire doit être connecté au CRM pour ouvrir le document.')}catch{}}const quick=docs.slice(0,2);return <main className="workspace documentsPage"><style>{`.documentsPage .docsHead{gap:30px;margin-bottom:30px}.documentsPage .docsHead .eyebrow{font-size:10px!important}.documentsPage .docsHead h1{font-size:38px}.documentsPage .docsHead .muted{font-size:14px!important;line-height:1.65;margin-top:9px}.documentsPage .uploadDoc{font-size:12px;min-height:42px;padding:11px 16px;flex:0 0 auto}.documentsPage .docsToolbar{gap:16px;padding:13px 15px}.documentsPage .docsSearch{font-size:15px;gap:9px}.documentsPage .docsSearch input{font-size:13px;padding:10px}.documentsPage .docFilters{gap:8px;flex-wrap:wrap}.documentsPage .docFilters button{font-size:11px;padding:8px 11px}.documentsPage .docsQuick{gap:16px;margin-top:18px}.documentsPage .docsQuick article{padding:20px;gap:15px}.documentsPage .docsQuick article>span{font-size:19px}.documentsPage .docsQuick small{font-size:9px!important;line-height:1.4;letter-spacing:.7px}.documentsPage .docsQuick b{display:block;font-size:14px;line-height:1.4;margin:5px 0}.documentsPage .docsQuick p{font-size:12px;line-height:1.55;margin:0}.documentsPage .docsQuick button{font-size:11px;min-height:36px;padding:8px 11px;margin-left:10px}.documentsPage .docsLibrary{margin-top:20px;padding:22px}.documentsPage .libraryTitle{margin-bottom:17px;gap:15px}.documentsPage .libraryTitle h2{font-size:20px}.documentsPage .libraryTitle span{font-size:11px}.documentsPage .docGrid{gap:16px}.documentsPage .docCard{min-width:0}.documentsPage .docVisual{min-height:150px}.documentsPage .docVisual>span{font-size:27px}.documentsPage .docVisual small{font-size:10px!important;line-height:1.35}.documentsPage .docInfo{padding:17px}.documentsPage .docInfo>small{font-size:9px!important;line-height:1.4;letter-spacing:.6px}.documentsPage .docInfo h3{font-size:15px;line-height:1.4;margin:7px 0}.documentsPage .docInfo p{font-size:12px;line-height:1.5;margin:0 0 14px}.documentsPage .docInfo footer{gap:9px;padding-top:12px}.documentsPage .docInfo footer button{font-size:11px;min-height:35px;padding:8px 10px}@media(max-width:760px){.documentsPage .docsHead{align-items:flex-start}.documentsPage .uploadDoc{width:100%}.documentsPage .docsToolbar{align-items:stretch}.documentsPage .docsQuick{grid-template-columns:1fr}.documentsPage .docsQuick article{align-items:flex-start}.documentsPage .docGrid{grid-template-columns:1fr}}`}</style><div className="docsHead"><div><p className="eyebrow">BIBLIOTHÈQUE COMMERCIALE</p><h1>Documents</h1><p className="muted">Tout ce dont vous avez besoin avant, pendant ou après un rendez-vous.</p></div><input ref={input} hidden type="file" accept=".pdf,.png,.jpg,.jpeg,.docx,.pptx" onChange={e=>{upload(e.target.files?.[0]);e.currentTarget.value=''}}/><button className="uploadDoc" disabled={blocked} onClick={()=>input.current?.click()}>＋ Ajouter un document</button></div><div className="docsToolbar"><div className="docsSearch">⌕ <input value={q} onChange={e=>setQ(e.target.value)} placeholder="Rechercher un document…"/></div><div className="docFilters">{['Tous','Présentation','Offre commerciale','Support RDV'].map(x=><button key={x} className={filter===x?'selected':''} onClick={()=>setFilter(x)}>{x==='Présentation'?'Présentations':x==='Offre commerciale'?'Offres':x==='Support RDV'?'Supports RDV':x}</button>)}</div></div>{quick.length>0&&<section className="docsQuick">{quick.map((d,i)=><article key={d.id}><span>{i?'↗':'★'}</span><div><small>{i?'DERNIER UTILISÉ':'ACCÈS RAPIDE'}</small><b>{d.name}</b><p>{d.fileName}</p></div><button onClick={()=>open(d)}>Ouvrir →</button></article>)}</section>}<section className="docsLibrary"><div className="libraryTitle"><h2>Bibliothèque</h2><span>{visible.length} documents</span></div><div className="docGrid">{visible.map((d,i)=><article className="docCard" key={d.id}><div className={`docVisual d${i%4}`}><span>{icon(d.category)}</span><small>RAPID<br/><b>PARE-BRISE</b></small></div><div className="docInfo"><small>{d.category.toUpperCase()}</small><h3>{d.name}</h3><p>{d.fileName} · {Math.max(1,Math.round(d.sizeBytes/1024))} Ko</p><footer><button onClick={()=>open(d)}>Voir</button><button onClick={()=>share(d)}>Partager ↗</button></footer></div></article>)}{!visible.length&&<p>{blocked?'Bibliothèque sécurisée indisponible. Aucun ajout n’est autorisé.':'Aucun document dans cette sélection.'}</p>}</div></section></main>}

import{NextResponse}from'next/server';import{databaseConfigured,db}from'../../../lib/db';import{authConfigured}from'../../../lib/auth';export const runtime='nodejs';export const dynamic='force-dynamic';const h={'Cache-Control':'no-store'};export async function GET(){if(!authConfigured())return NextResponse.json({ok:false,database:databaseConfigured(),auth:false,message:'Accès sécurisé non configuré.'},{status:503,headers:h});if(!databaseConfigured())return NextResponse.json({ok:false,database:false,auth:true,message:'Base PostgreSQL non configurée. Les écritures sont bloquées.'},{status:503,headers:h});try{await db()`select 1`;return NextResponse.json({ok:true,persistence:'postgresql',database:true,auth:true,message:'Persistance serveur et verrou de sécurité actifs.'},{headers:h})}catch{return NextResponse.json({ok:false,persistence:'unavailable',database:true,auth:true,message:'La base est configurée mais indisponible. Les écritures restent bloquées.'},{status:503,headers:h})}}

import {NextRequest,NextResponse} from 'next/server';import {databaseConfigured,db} from '../../../lib/db';import{authConfigured,sessionValid}from'../../../lib/auth';
export const runtime='nodejs';export const dynamic='force-dynamic';const noStore={'Cache-Control':'no-store'};async function authorized(){return authConfigured()&&await sessionValid()}
export async function GET(){if(!databaseConfigured())return NextResponse.json({mode:'local-demo',items:[]},{headers:noStore});if(!authConfigured())return NextResponse.json({error:'secure_access_not_configured'},{status:503,headers:noStore});if(!(await sessionValid()))return NextResponse.json({error:'unauthorized'},{status:401,headers:noStore});try{const rows=await db()`select id,name,sector,zone,address,fleet,phone,status,score,notes,next_action as "next",access,insurance,potential_revenue as "potentialRevenue",signed_revenue as "signedRevenue",generated_revenue as "generatedRevenue",updated_at as "updatedAt" from prospects where deleted_at is null order by updated_at desc`;return NextResponse.json({mode:'postgresql',items:rows},{headers:noStore})}catch{return NextResponse.json({error:'database_unavailable'},{status:503,headers:noStore})}}
export async function POST(req:NextRequest){if(!databaseConfigured())return NextResponse.json({error:'database_not_configured'},{status:503,headers:noStore});if(!authConfigured())return NextResponse.json({error:'secure_access_not_configured'},{status:503,headers:noStore});if(!(await authorized()))return NextResponse.json({error:'unauthorized'},{status:401,headers:noStore});let x:any;try{x=await req.json()}catch{return NextResponse.json({error:'invalid_request'},{status:400,headers:noStore})}const name=String(x.name||'').trim();const score=Number(x.score??50);if(!name||!Number.isFinite(score)||score<0||score>100)return NextResponse.json({error:'invalid_prospect'},{status:400,headers:noStore});try{const rows=await db().begin(async sql=>{const created=await sql`insert into prospects(name,sector,zone,address,fleet,phone,status,score,notes,next_action,access,insurance,potential_revenue,signed_revenue,generated_revenue) values(${name},${String(x.sector||'')},${String(x.zone||'')},${String(x.address||'')},${String(x.fleet||'')},${String(x.phone||'')},${String(x.status||'Nouveau')},${score},${String(x.notes||'')},${String(x.next||'')},${String(x.access||'À vérifier')},${String(x.insurance||'À vérifier')},${Number(x.potentialRevenue)||0},${Number(x.signedRevenue)||0},${Number(x.generatedRevenue)||0}) returning id,name,sector,zone,address,fleet,phone,status,score,notes,next_action as "next",access,insurance,potential_revenue as "potentialRevenue",signed_revenue as "signedRevenue",generated_revenue as "generatedRevenue",updated_at as "updatedAt"`;await sql`insert into prospect_events(prospect_id,event_type,payload) values(${created[0].id},'created',${sql.json({source:'crm'})})`;return created});return NextResponse.json({item:rows[0]},{status:201,headers:noStore})}catch{return NextResponse.json({error:'database_unavailable'},{status:503,headers:noStore})}}

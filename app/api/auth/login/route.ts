import{NextRequest,NextResponse}from'next/server';import{authConfigured,createSession,passwordValid,SESSION_COOKIE}from'../../../../lib/auth';export const runtime='nodejs';export async function POST(req:NextRequest){if(!authConfigured())return NextResponse.json({error:'secure_access_not_configured'},{status:503});let password='';try{password=String((await req.json()).password||'')}catch{return NextResponse.json({error:'invalid_request'},{status:400})}if(!passwordValid(password))return NextResponse.json({error:'invalid_credentials'},{status:401});const session=createSession();const res=NextResponse.json({ok:true});res.cookies.set(SESSION_COOKIE,session.value,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'strict',path:'/',maxAge:session.maxAge});return res}

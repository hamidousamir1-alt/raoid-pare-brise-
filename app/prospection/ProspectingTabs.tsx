'use client';import{ReactNode,useState}from'react';export default function ProspectingTabs({business,individuals}:{business:ReactNode;individuals:ReactNode}){const[tab,setTab]=useState<'business'|'individuals'>('business');return <><div className="workspacebar" style={{marginBottom:10,justifyContent:'flex-start'}}><button className={tab==='business'?'primary':''} onClick={()=>setTab('business')}>Entreprises</button><button className={tab==='individuals'?'primary':''} onClick={()=>setTab('individuals')}>Particuliers</button></div>{tab==='business'?business:individuals}</>}
